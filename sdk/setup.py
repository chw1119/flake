from setuptools import setup, find_packages

setup(
    name='flake-sdk',
    version='1.0.0',
    description='Python SDK for Flake memo app',
    packages=find_packages(),
    python_requires='>=3.8',
)
